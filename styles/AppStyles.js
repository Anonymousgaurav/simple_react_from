import { StyleSheet } from "react-native"

export default formStyles = StyleSheet.create(
    {
        externalStyle : {
            color : '#000000',
            fontSize : 25,
            backgroundColor : 'white'
        }
    }
)